services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: genpicks
      POSTGRES_PASSWORD: genpicks
      POSTGRES_DB: genpicks
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
